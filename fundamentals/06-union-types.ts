let uniqueIdentifier: number | string = 1000;
uniqueIdentifier = "214efs-3213dfg-54653fg"

const keys: (number | string) [] = [1, 3, 'dasf']

let courseId: number | null = 1000

courseId = null

// this union type is used only when the type of variable can change like from null to number or number to string etc.  