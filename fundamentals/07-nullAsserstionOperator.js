let newCourseId;
newCourseId.toString();
newCourseId.toString(); // !(non null assertion operator) character tells the typescript compiler that the variable is not null. it surely has a value.
// this can be used to bypass the strict null check in ts. if you are confident that a variable should have a value.
// number or null
