// lesser known, used for RX js library.

// its just like an array where the ts defines certain data types should be in certain position.

type CourseRecordTuple = [string, string, number];

const courseRecord : CourseRecordTuple = ['TS Bootcamp', 'Learn Ts fundamentals', 200]